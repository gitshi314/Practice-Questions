#include <stdio.h>
int main() {
    int num,average,deviation;
    int value[num];
    printf("Enter the number: ");
    scanf("%d", &num);
    printf("Enter the values: ");
    for (int i = 0; i < num; i++) {
        scanf("%d", &value[i]);
    }
    
}
